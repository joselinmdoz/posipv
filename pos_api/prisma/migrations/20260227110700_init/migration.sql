-- NOTE:
-- This migration was accidentally generated as a duplicated baseline snapshot.
-- All objects defined here were already introduced by previous migrations,
-- and running this file causes duplicate-object errors in production.
--
-- Keep it as a no-op to preserve migration ordering/history.
SELECT 1;
